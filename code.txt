import asyncioimport aiohttpfrom bs4 import BeautifulSoupimport urllib.parseimport reimport loggingimport jsonfrom datetime import datetimelogging.basicConfig(level=logging.INFO, format='%(asctime)s - [%(levelname)s] - %(message)s')class BulletproofBountyHunter:def init(self, base_url):parsed_base = urllib.parse.urlparse(base_url)self.domain = parsed_base.netloc if parsed_base.netloc else parsed_base.pathself.domain = self.domain.replace("www.", "")self.base_url = f"https://{self.domain}"self.discovered_endpoints = set()self.js_files = set()self.subdomains = set([self.domain])self.report_data = {"target": self.base_url,"scan_time": datetime.now().strftime("%Y-%m-%d %H:%M:%S"),"subdomains": [],"discovered_apis": [],"leaked_secrets": [],"vulnerabilities": []}self.headers = {'User-Agent': 'Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/122.0.0.0 Safari/537.36','Accept': 'text/html,application/xhtml+xml,application/xml;q=0.9,image/avif,image/webp,/;q=0.8','X-Forwarded-For': '127.0.0.1','X-Originating-IP': '127.0.0.1','X-Remote-IP': '127.0.0.1','X-Client-IP': '127.0.0.1'}async def create_session(self):timeout = aiohttp.ClientTimeout(total=15)return aiohttp.ClientSession(headers=self.headers, timeout=timeout)async def enumerate_subdomains(self, session):logging.info(f"Launching Asynchronous Subdomain Enumeration for: {self.domain}")crt_url = f"crt.sh%.{self.domain}&output=json"try:async with session.get(crt_url, timeout=20) as response:if response.status == 200:try:data = await response.json()for item in data:name_value = item.get('name_value', '')for sub in name_value.split('\n'):sub = sub.replace('.', '').strip().lower()if sub and sub.endswith(self.domain):self.subdomains.add(sub)except json.JSONDecodeError:text_data = await response.text()found = re.findall(r'"name_value":"([^"]+)"', text_data)for sub in found:sub = sub.replace('.', '').strip().lower()if sub and sub.endswith(self.domain):self.subdomains.add(sub)logging.info(f"[+] Total Clean Subdomains Discovered: {len(self.subdomains)}")self.report_data["subdomains"] = list(self.subdomains)except Exception as e:logging.error(f"[-] Subdomain Enumeration pipeline timed out or was blocked: {str(e)}")async def extract_endpoints_and_js(self, session, url):logging.info(f"Crawl Engine Executing on Target Vector: {url}")try:async with session.get(url, allow_redirects=True) as response:if response.status != 200: returnhtml = await response.text()soup = BeautifulSoup(html, 'html.parser')for a in soup.find_all('a', href=True):href = a['href']full_url = urllib.parse.urljoin(url, href)parsed_item = urllib.parse.urlparse(full_url)if any(sub in parsed_item.netloc for sub in self.subdomains):self.discovered_endpoints.add(full_url)for script in soup.find_all('script', src=True):src = script['src']full_js_url = urllib.parse.urljoin(url, src)parsed_js = urllib.parse.urlparse(full_js_url)if any(sub in parsed_js.netloc for sub in self.subdomains):self.js_files.add(full_js_url)except Exception as e:logging.debug(f"Gracefully skipped unreachable crawl trajectory {url}: {str(e)}")async def analyze_js_files(self, session):logging.info(f"Analyzing {len(self.js_files)} Client-Side Structural JS Component Layers...")api_pattern = re.compile(r'"'["']')secret_pattern = re.compile(r'(secret|api_key|token|bearer|credential|aws_signing|private_key).{0,10}"'["']', re.IGNORECASE)for js_url in list(self.js_files):try:async with session.get(js_url) as response:if response.status != 200: continuecontent = await response.text()apis = api_pattern.findall(content)for api in apis:clean_api = api.strip('"'')found_route = urllib.parse.urljoin(self.base_url, clean_api)if found_route not in self.discovered_endpoints:logging.warning(f" [API ROUTE DISCOVERED IN JS]: {clean_api}")self.discovered_endpoints.add(found_route)self.report_data["discovered_apis"].append({"file": js_url, "route": clean_api})secrets = secret_pattern.findall(content)for sec in secrets:logging.critical(f" [CRITICAL - SECRET LEAK IDENTIFIED]: Bundle {js_url} Parameter: {sec} -> {sec[:6]}")self.report_data["leaked_secrets"].append({"file": js_url, "type": str(sec), "snippet": f"{str(sec)[:6]}"})except Exception:passasync def analyze_xss_context(self, session, url):context_marker = "hunterXSStag"'><"parsed_url = urllib.parse.urlparse(url)params = urllib.parse.parse_qs(parsed_url.query)if not params: returnfor param in params:test_params = params.copy()test_params[param] = [context_marker]new_query = urllib.parse.urlencode(test_params, doseq=True)test_url = parsed_url._replace(query=new_query).geturl()try:async with session.get(test_url) as response:body = await response.text()if context_marker in body:if f"value="{context_marker}"" in body or f"value='{context_marker}'" in body:msg = f"Attribute context reflection discovered on '{param}' parameter. Quotes unescaped."logging.warning(f" [XSS RISK]: {msg} at {url}")self.report_data["vulnerabilities"].append({"type": "Reflected XSS (Attribute Context)", "url": url, "details": msg})elif f"<{context_marker}" in body or context_marker in body:msg = "Direct raw text node script execution injection bounds open."logging.critical(f" [HIGH XSS RISK]: {msg} at {url}")self.report_data["vulnerabilities"].append({"type": "Reflected XSS (HTML Context)", "url": url, "details": msg})except Exception:passasync def check_serialization_and_tokens(self, session, url):try:async with session.get(url) as response:cookies = response.cookiesfor name, cookie in cookies.items():val = cookie.valueif val.startswith("rO0AB") or "rO0AB" in urllib.parse.unquote(val):msg = f"Java Native Serialization Object discovered tracking state inside session cookie '{name}'."logging.critical(f" [CRITICAL DESERIALIZATION]: {msg} at {url}")self.report_data["vulnerabilities"].append({"type": "Insecure Deserialization (Java)", "url": url, "details": msg})if "O:" in urllib.parse.unquote(val) and "%3A" in val:msg = f"PHP Native Serialization footprint payload tracked inside parameter cookie '{name}'."logging.critical(f" [CRITICAL DESERIALIZATION]: {msg} at {url}")self.report_data["vulnerabilities"].append({"type": "Insecure Deserialization (PHP)", "url": url, "details": msg})except Exception:passasync def check_gateways(self, session, url):ssrf_indicators = ['url', 'dest', 'redirect', 'uri', 'path', 'domain', 'callback', 'webhook', 'proxy']idor_indicators = ['id', 'user', 'account', 'invoice', 'doc', 'profile', 'uuid', 'uid']parsed = urllib.parse.urlparse(url)params = urllib.parse.parse_qs(parsed.query)for param in params.keys():p_lower = param.lower()if any(ind in p_lower for ind in ssrf_indicators):msg = f"Parameter '{param}' handles structural routing values. Intercept payload maps via Burp Suite and probe cloud configuration metadata endpoints."logging.warning(f" [SSRF GATEWAY MAP]: {msg} on {url}")self.report_data["vulnerabilities"].append({"type": "Potential SSRF Gateway Entry", "url": url, "details": msg})if any(idor in p_lower for idor in idor_indicators):val = params[param]msg = f"Exposed structural reference identifier vector logic discovered: '{param}'={val}. Manipulate sequence matrices to test authentication authorization controls."logging.warning(f" [IDOR / REFERENCE ENTRY]: {msg} on {url}")self.report_data["vulnerabilities"].append({"type": "Potential IDOR Tracking Pattern", "url": url, "details": msg})def generate_markdown_report(self):filename = f"bug_bounty_report_{self.domain}.md"logging.info(f"Writing findings into automated reporting layer file: {filename}")with open(filename, "w", encoding="utf-8") as f:f.write(f"# 🛡️ Enterprise Vulnerability Intelligence Scan Report\n\n")f.write(f"- Target Domain Base: {self.report_data['target']}\n")f.write(f"- Scan Execution Timestamp: {self.report_data['scan_time']}\n\n")f.write(f"---\n\n## 🌐 1. Discovered Subdomain Infrastructure Assets ({len(self.report_data['subdomains'])})\n")for sub in self.report_data["subdomains"]:f.write(f"- {sub}\n")f.write(f"\n## 🔑 2. Hardcoded Static Secret Leaks Found ({len(self.report_data['leaked_secrets'])})\n")if not self.report_data["leaked_secrets"]:f.write("No open hardcoded crypto keys or secrets exposed directly within the global static script bundles.\n")for sec in self.report_data["leaked_secrets"]:f.write(f"### 🔴 Critical Danger Exposure\n- Source Bundle URL: {sec['file']}\n- Pattern Indicator Tag: {sec['type']}\n- Masked Content Signature: {sec['snippet']}\n\n")f.write(f"## ⚙️ 3. Client-Side Discovered API System Routes ({len(self.report_data['discovered_apis'])})\n")if not self.report_data["discovered_apis"]:f.write("No unmapped hidden system routing strings exposed inside internal frontend files.\n")for api in self.report_data["discovered_apis"]:f.write(f"- Route discovered inside {api['file']} -> {api['route']}\n")f.write(f"\n## ⚠️ 4. Architecture Vulnerability Footprints Identified ({len(self.report_data['vulnerabilities'])})\n")if not self.report_data["vulnerabilities"]:f.write("No high-risk edge cases or logic reflection vulnerabilities identified out-of-the-box by static behavioral processing tests.\n")for vuln in self.report_data["vulnerabilities"]:f.write(f"### 🚨 [{vuln['type']}]\n- Target Vector URL: {vuln['url']}\n- Vulnerability Technical Details: {vuln['details']}\n\n")logging.info(f"[+] Operational Security Logging Completed. Report file structure sealed: {filename}")async def hunt(self):async with await self.create_session() as session:await self.enumerate_subdomains(session)logging.info("Initializing Network Probing Context Infrastructure Arrays...")crawl_targets = [f"https://{sub}" for sub in list(self.subdomains)[:15]]crawl_tasks = [self.extract_endpoints_and_js(session, target) for target in crawl_targets]await asyncio.gather(*crawl_tasks)if not self.discovered_endpoints:self.discovered_endpoints.add(self.base_url)logging.info(f"Total Unique In-Scope Endpoints Undergoing Processing: {len(self.discovered_endpoints)}")await self.analyze_js_files(session)logging.info("Spinning Asynchronous Parallel Interception Attack Surface Matrix...")tasks = []for url in list(self.discovered_endpoints):tasks.append(self.analyze_xss_context(session, url))tasks.append(self.check_serialization_and_tokens(session, url))tasks.append(self.check_gateways(session, url))sem = asyncio.Semaphore(5)async def safe_task(task):async with sem:return await taskawait asyncio.gather(*[safe_task(t) for t in tasks])self.generate_markdown_report()print("\n" + "="*70 + f"\n[+] Processing Engine Finalized Successfully! View your report: bug_bounty_report_{self.domain}.md\n" + "="*70)if name == "main":target_scope = "https://example.com"hunter = BulletproofBountyHunter(target_scope)asyncio.run(hunter.hunt())